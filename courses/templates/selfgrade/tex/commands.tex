\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{xr}
\usepackage{graphicx}
\usepackage{enumitem}
\usepackage{bm}
\usepackage{cancel}
\usepackage{hyperref}
\hypersetup{colorlinks=true,linkcolor=blue,urlcolor=blue}

\newlist{problems}{enumerate}{2}
\setlist[problems,1]{label=\arabic*}
\setlist[problems,2]{label=(\emph{\alph*})}

{# raw because of the symbol { then # needed  #}
{% raw %}
\newcommand{\pd}{\partial}
\newcommand{\mat}[1]{\begin{pmatrix} #1 \end{pmatrix}}
\newcommand{\mm}[1]{\underline{\mathbf{#1}}}
{% endraw %}

{% graphicspath %}

{# relative path does not work; raw is needed  #}
{# And actually even this doesn't work on servier for some reason #}
{# I copied and pasted the absolute path into the CM.auxil file there ... #}

\externaldocument{ {{- aux_absolute_path -}} }
